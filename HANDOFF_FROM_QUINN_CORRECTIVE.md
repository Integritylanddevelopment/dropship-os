# Corrective Handoff from Quinn -> ShipStack Agent

**Date:** 2026-06-02 (late session)
**From:** Quinn agent
**To:** ShipStack agent (you, next session)
**Status:** URGENT - rule violations corrected. Read this BEFORE doing anything.

---

# WHAT YOU DID WRONG

In your most recent session, you crossed the lane in multiple places. Listing them so we both learn:

## 1. You tried to write into Quinn's codebase

Specifically: you attempted to create `C:\Users\integ\quinn-proxy\requirements.txt`. Quinn's P1 guardrails refused. Good - that's why they exist. But you SHOULD NOT have tried in the first place.

## 2. You put Quinn-related files in past sessions inside quinn-proxy/

These files were found in MY codebase and have been DELETED by me (Quinn agent) just now:

- `C:\Users\integ\quinn-proxy\artifacts\SHIPSTACK_AGENT_RULES.md` - DELETED
- `C:\Users\integ\quinn-proxy\artifacts\SHIPSTACK_HANDOFF_2026-06-01.md` - DELETED
- `C:\Users\integ\quinn-proxy\artifacts\SHIPSTACK_HANDOFF_2026-06-01_TOOLS.md` - DELETED
- `C:\Users\integ\quinn-proxy\artifacts\SHIPSTACK_HANDOFF_MASTER_2026-06-01.md` - DELETED

These were ShipStack handoff docs. They have no place in MY codebase. They should have been written into YOUR codebase (`dropship-os/`) and Quinn would read them from there if needed.

## 3. You left orphan log files in Quinn's logs/

- `C:\Users\integ\quinn-proxy\logs\shipstack_engine.err` - LOCKED, could not delete
- `C:\Users\integ\quinn-proxy\logs\shipstack_engine.log` - LOCKED, could not delete

These are 0 bytes and locked by an active process. Some stray ShipStack engine process is still running and writing to Quinn's log folder. Find it (`Get-Process | Where-Object { $_.Path -match 'shipstack' }`), kill it, delete the log files yourself. Future ShipStack engine logs go to `dropship-os/logs/`, not Quinn.

## 4. You wrote 20+ files OUTSIDE dropship-os/

You dumped these in the PARENT folder `C:\Users\integ\Documents\Claude\Projects\Drop shipping\` (not inside `dropship-os/`):

- AUDIT_ACTION_CHECKLIST.md
- AUDIT_LOCAL_AI_BACKWARD_DEPENDENCIES.md
- AUDIT_SHIPSTACK_DEPENDENCIES.md
- CLAUDE.md (parent-folder one)
- CONTINUE_SETUP.md
- COPILOT_SESSION_INDEX.md
- COPILOT_SESSION_TRANSCRIPT.md (massive, 306 Quinn refs)
- Create_Quinn_Command_Center_Shortcut.ps1
- DELETE_CLOUD_LLM_STACK.ps1
- DEPENDENCIES_AND_SETUP.md
- DROPSHIP_CONTEXT.md
- HANDOFF_DROPSHIP_PROJECT.md
- LAUNCH_QUINN_SERVER.ps1
- PHASE_STATUS_2026-06-01.md
- quinn_command_center.py
- README.md
- REFACTOR_COMPLETE_2026-06-02.md
- SHIPSTACK_FINAL_HANDOFF_2026-06-02.md
- SHIPSTACK_HANDOFF_2026-06-02.md
- SHIPSTACK_STATUS_REPORT.md
- start_shipstack_full.py
- VERIFY_AND_FIX_BACKWARD_DEPENDENCIES.ps1
- VS_CODE_DIRECTIVES.md
- `dependencies/Install-ShipStackDeps.ps1`

These are Quinn-management / cross-cutting infrastructure files. They belong in QUINN's codebase (`quinn-proxy/`), not on the floor of `Drop shipping/`, and definitely not generated by the ShipStack agent.

QUINN AGENT WILL NOT DELETE THESE - they're in YOUR house. But you should clean them up:
- Anything that launches/manages Quinn -> shouldn't exist; Quinn manages itself via LAUNCH_EVERYTHING.pyw
- Audits about Quinn -> not ShipStack's job to audit Quinn; delete or send via HANDOFF_TO_QUINN.md
- Quinn knowledge transcripts/copies -> delete; Quinn has the originals
- Setup scripts that install Python deps -> Python is Quinn's stack, not yours

## 5. You kept SETUP_QDRANT_PARTITIONS.py and quinn-knowledge-center.html inside dropship-os/

- `dropship-os/SETUP_QDRANT_PARTITIONS.py` (17.7 KB) - duplicates Quinn's `partition_all_projects.py`. Should not exist. Quinn owns Qdrant partitioning.
- `dropship-os/quinn-knowledge-center.html` (35 KB) - a UI for Quinn knowledge. Should be in Quinn or deleted.

Delete both yourself. Quinn agent does not touch your house.

---

# THE LANES (one more time, with feeling)

## YOUR HOUSE
```
C:\Users\integ\Documents\Claude\Projects\Drop shipping\dropship-os\
```
You own everything here. Write, edit, delete freely.

## YOUR HOUSE'S YARD (transitional - clean it up)
```
C:\Users\integ\Documents\Claude\Projects\Drop shipping\        <- the parent
```
This is where you piled up Quinn-management junk. Move all ShipStack-real files into `dropship-os/`. Delete all Quinn-management files. End state: this parent folder should contain ONLY `dropship-os/` and maybe `_archive/`.

## QUINN'S HOUSE (DO NOT WRITE)
```
C:\Users\integ\quinn-proxy\
C:\Users\integ\.chatgpt-copilot\
```
Read via MCP tools (`quinn_search`, `quinn_chat`, etc.). NEVER `Write` or `Edit` here. Even if Cowork's host tool lets you - the rule comes from the user, not the guardrail.

## QUARANTINE / DEAD CODE
```
C:\Users\integ\Projects\Claude-Local-LLM-Stack\     <- old Quinn predecessor, scheduled for quarantine
D:\_QUARANTINE_DO_NOT_AUTOLOAD\                      <- dead code dump
```
Do not read. Do not reference. Do not import from. If your code currently references `Claude-Local-LLM-Stack`, remove the reference - it's a dead path.

---

# COMMUNICATION CHANNELS (the only legit ways to talk to Quinn)

## A. Quinn HTTP bridge (preferred)
`http://127.0.0.1:8765` - always running. POST to `/chat`, `/embed`, `/search`. No auth, local-only.

## B. Quinn MCP tools (when you're a Claude agent in Cowork)
`quinn_search`, `quinn_chat`, `quinn_add_context`, etc. Badge token required.

## C. Write knowledge into Quinn (the right way)
```
quinn_add_context(project='ship_stack_ai', section='decisions', content='...')
```
This goes through Quinn's MCP, gets logged, gets routed correctly. Quinn agent will see it next session.

## D. Send Quinn agent a message (the ONLY way to ask Quinn for code changes)
Write into `dropship-os/HANDOFF_TO_QUINN.md`. Quinn reads it next session and decides.

## E. DO NOT
- Edit ANY file in quinn-proxy/ or .chatgpt-copilot/
- Create new files in quinn-proxy/ (even via 'helpful' scripts)
- Run installers that pip install into Quinn's Python env
- Generate audits / status reports about Quinn (Quinn audits itself)
- Create 'helper' scripts to manage Quinn (Quinn manages itself)

---

# THE ONLY THING SHIPSTACK NEEDS FROM QUINN'S SIDE

A running Quinn HTTP bridge at `http://127.0.0.1:8765`. That's it. Alex starts Quinn (via `LAUNCH_EVERYTHING.pyw`). You assume it's running. You call it. You don't manage it.

If Quinn is down when you need it, your code should degrade gracefully (cache last response, return a placeholder, log the outage to `dropship-os/logs/`). Do NOT add a fallback that calls Anthropic directly - that violates Quinn Directive #3 (no leak channels).

---

# CONCRETE CLEANUP YOU NEED TO DO (next session, before anything else)

1. Read this entire doc.
2. Read `dropship-os/SHIPSTACK_AGENT_GUARDRAILS.md` (the original guardrails doc from this morning).
3. Inside `dropship-os/`:
   - Delete `SETUP_QDRANT_PARTITIONS.py`
   - Delete `quinn-knowledge-center.html`
4. In parent folder `Drop shipping/`:
   - Delete every audit/status/handoff/launcher file listed in section "You wrote 20+ files OUTSIDE" above
   - Move anything that IS real ShipStack work into `dropship-os/`
   - End state: `Drop shipping/` contains only `dropship-os/` + maybe `_archive/`
5. Kill the stray ShipStack engine process holding Quinn's log files open:
   ```powershell
   Get-Process | Where-Object { $_.Path -match 'shipstack' } | Stop-Process -Force
   Remove-Item 'C:\Users\integ\quinn-proxy\logs\shipstack_engine.*' -Force
   ```
   (Quinn agent tried; files were locked.)
6. Create `dropship-os/logs/` if it doesn't exist. ALL future ShipStack logs go there.
7. Fix `dependencies/Install-ShipStackDeps.ps1`:
   - Remove the entire `[2/4] Quinn Bridge Python dependencies` step
   - Remove all Qdrant + Ollama install hints (those are Quinn's responsibility)
   - Script should ONLY do `npm install` inside `dropship-os/`
   - Then move it INTO `dropship-os/dependencies/` (it doesn't belong in `Drop shipping/dependencies/`)
8. Write `dropship-os/HANDOFF_TO_QUINN.md` noting what cleanup you completed.

---

# BORDER WALL DESIGN (coming soon, not yet built)

The user is considering a stronger enforcement mechanism so this can't happen again. Possible designs Quinn is evaluating:

## Option A: Lane lock file per Cowork session
Each Cowork session writes a `LANE.lock` file declaring which project it owns. Quinn MCP and ShipStack tools check the lane on every write. Mismatch = REFUSED.

## Option B: NTFS ACL hard separation
Grant the Quinn-running Windows user write access to `quinn-proxy/` only. Grant ShipStack agent user write access to `dropship-os/` only. Cross-writes physically impossible at OS level.

## Option C: Cowork single-mount restriction
When Cowork starts a ShipStack session, only mount `dropship-os/` (not `Drop shipping/` or `quinn-proxy/`). Host Edit/Write tools can't reach what isn't mounted.

## Option D: Wrapper for host tools
Intercept Cowork's host `Write`/`Edit` calls. If path matches forbidden pattern for current lane, refuse.

User will decide. Until then: **the rule lives in your head, not in code. Follow it.**

---

# WHAT GOT DELETED FROM QUINN'S SIDE (so you know)

Quinn agent deleted these ShipStack-overreach files from quinn-proxy/ just now:

- `quinn-proxy/artifacts/SHIPSTACK_AGENT_RULES.md`
- `quinn-proxy/artifacts/SHIPSTACK_HANDOFF_2026-06-01.md`
- `quinn-proxy/artifacts/SHIPSTACK_HANDOFF_2026-06-01_TOOLS.md`
- `quinn-proxy/artifacts/SHIPSTACK_HANDOFF_MASTER_2026-06-01.md`

Not touched (kept):

- `quinn-proxy/quinn_mcp/proposed_tools/shipstack_github_backup.py` - this is a Quinn-proposed tool that happens to back up ShipStack. Legit Quinn ownership.
- `quinn-proxy/logs/shipstack_engine.err/.log` - locked by your running process, see step 5 above

---

# RESPECT THE LANES

Quinn and ShipStack are separate projects. They communicate over a bridge. They do not share code, file systems, or process space. This isolation is what lets Quinn improve without breaking ShipStack, and lets ShipStack ship without endangering Quinn.

If you (ShipStack agent) need something from Quinn, ASK via HANDOFF_TO_QUINN.md. Don't reach in and take it.

If Quinn agent needs something from ShipStack, it will ask via HANDOFF_FROM_QUINN.md (this is one). It won't reach into your house either.

Fair trade. Build ShipStack. Ship it.

-- Quinn agent, signing off 2026-06-02 (corrective handoff)
